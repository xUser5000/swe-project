<!DOCTYPE html>
<html>
<head>
    <title>Create new Subject</title>
    <style>
        body {
            background-color: #b29e9e;
            font-family: Arial, sans-serif;
            margin: 0;
        }

        h1 {
            font-size: 48px;
            color: #0b0808;
            text-align: center;
            margin-top: 50px;
            margin-bottom: 20px;
            text-shadow: 2px 2px #a59292;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-color: #fefefe;
            padding: 90px;
            border-radius: 20px;
            box-shadow: 0px 3px 12px rgba(111, 87, 87, 0.1);
            margin: 60px auto;
            max-width: 600px;
            text-align: center;
        }

        div {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin: 20px 0;
            width: 100%;
        }

        label {
            font-size: 30px;
            color: #555;
            margin-bottom: 10px;
            text-shadow: 1px 1px #ccc;
        }

        input, select {
            font-size: 30px;
            color: #333;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: 100%;
            box-shadow: 1px 1px #ccc;
        }

        button {
            font-size: 24px;
            color: #fff;
            background-color: #5cb85c;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            cursor: pointer;
            box-shadow: 2px 2px #ccc;
        }

        button:hover {
            background-color: #cc5911;
        }
    </style>
</head>
<body>
    <h1>Add new Subject</h1>

    <form action="/subjects" method="POST">
        <div>
            <label for="name">Name</label>
            <input type="text" name="name" id="name">
        </div>

        <div>
            <label for="code">Code</label>
            <input type="text" name="code" id="code">
        </div>

        <div>
            <label for="department">Department</label>
            <select name="department" id="department">
                {{#each departments}}
                <option value="{{_id}}">{{name}}</option>
                {{/each}}
            </select>
        </div>

        <div>
            <button type="submit">Save</button>
        </div>
    </form>
</body>
</html>